name: fluter_sound
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  win32: ^5.0.9
  provider: ^6.0.5

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets: []
